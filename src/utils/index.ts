import { performCalculation } from '@/utils/calculations';
import { DECIMAL_PART_LIMIT, INTEGER_PART_LIMIT } from '@/utils/consts';

export { performCalculation, DECIMAL_PART_LIMIT, INTEGER_PART_LIMIT };
